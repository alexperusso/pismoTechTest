spring:
  config:
    activate:
      on-profile: local

  zipkin:
    enabled: false

  sleuth:
    sampler:
      percentage: 0

  data:
    mongodb:
      host: localhost
      port: 27017
      authentication-database: admin
      username: root
      password: root
      database: cartoes

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetch-registry: false